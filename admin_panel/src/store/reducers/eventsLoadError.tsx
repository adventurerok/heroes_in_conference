import {AppObjectAction} from "../AppActions";


export function reduceEventsLoadError(state: string | undefined, action: AppObjectAction): string | undefined {
    switch(action.type) {
        default: {
            return state;
        }
    }
}